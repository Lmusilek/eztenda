


<div class="banner-small">
  <div class="container">
  </div>
</div>
<div class ="container">
  
  <div class="margin-top-40 d-flex">
    <div class="col-md-4 p-0">
      <h1><%= @venue.venue_name %></h1>
      <h3>Venue type</h3>
    </div>

    <div class="venue-logo col-md-6">
         <%= cl_image_tag @venue.photo.key %>
    </div>
  </div>


    <div class ="body-flex">
    

      <div class="flex-left">
        <p><strong>About</strong></p>
        <p><%= @venue.description %></p>
        <p> Lorem ipsum dolor sit amet, consectetur adipisicing elit. Harum id ab perferendis sequi,
        odit quo est maxime modi nobis, quis quod necessitatibus culpa aspernatur, praesentium
        optio qui atque eius. Laboriosam!</p>
      </div>
      
      <div class="flex-right">
          <p><strong>Location</strong></p>
          <p><%= @venue.region %></p>
          <p><%= @venue.city %></p>
          <p><%= @venue.post_code %></p>
          <% if @venue.user == current_user %>
            <%= link_to "Edit", edit_venue_path(@venue), class: 'mt-4  btn btn-warning'%>
            <%= link_to "Delete", venue_path(@venue), class: 'mt-4  btn btn-secondary', method: :delete, data: { confirm: 'Are you sure that you want to delete this venue?' } %>
          <% end %>
      </div>
  </div>
</div>

 <div class ="container">
    <div>
      <h2>Listings:</h2>
      <p>Item:</p>
      <p>Period:</p>
      <p>Description:</p>
      <p>Price:</p>
    </div>

    <div>
      <%= submit_tag 'Bid', class: 'btn btn-warning' %>
    </div>
  </div>

   <div class ="container mt-4">
    <div>
      <h2>Bidding History</h2>
    </div>
  </div>
  
